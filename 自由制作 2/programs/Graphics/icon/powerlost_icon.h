#ifndef Powerlost_icon_H
#define Powerlost_icon_H

typedef struct powerlost_icon_parameters{
    int layer;
    int powerlost_stage;
}powerlost_icon_parameters;
int powerlost_icon(int layer,int powerlost_stage);
#endif